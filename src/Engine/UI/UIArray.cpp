#include "UI/Array.h"

namespace UI
{
    Array::Array(const Vector2i& offset, const Vector2i& pixelPos)
    {
    }

    Array::Array(const Vector2i& offset, const Vector2i& pixelPos, unsigned int gridSize)
    {
    }

    Array::~Array()
    {
    }

    void Array::Draw()
    {
    }

    void Array::OnTransform()
    {
    }
}