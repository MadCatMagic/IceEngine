#include "UI.h"

namespace UI
{
    Grid::Grid(const Vector2i& size, const Vector2i& pixelPos)
    {
    }

    Grid::Grid(const Vector2i& size, const Vector2i& pixelPos, const Vector2i& gridSize)
    {
    }

    Grid::~Grid()
    {
    }

    void Grid::Draw()
    {
    }

    void Grid::OnTransform()
    {
    }
}